-- 입장 점검 문항 1 — 단일 테이블 조회
--
-- books 테이블에서 분야(category)가 '과학'이고 정가(price)가 20000 이상인 책을 골라,
-- 제목(title)과 정가(price) 두 열을 보여 주세요.
--   열 이름: title, price (그대로)
--   정렬: 정가 내림차순, 정가가 같으면 제목 오름차순
--
-- 아래에 SQL 을 적고 ./entry_check.sh 를 실행하세요.

