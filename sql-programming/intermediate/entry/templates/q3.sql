-- 입장 점검 문항 3 — 집계
--
-- books 테이블을 분야(category)별로 묶어, 분야 이름·책 수·평균 정가(정수로 반올림)를
-- 보여 주세요.
--   열 이름: category, book_count, avg_price (그대로)
--   평균 정가: round(avg(price)) 처럼 정수로 반올림한 값
--   정렬: 분야 이름 오름차순
--
-- 아래에 SQL 을 적고 ./entry_check.sh 를 실행하세요.

